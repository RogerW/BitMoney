Polymer 'app-bar',
  ready: ->
    @items = []
    #_nodes = @$.menu__links.getDistributedNodes()
    #i = 0
    #while i < _nodes.length
      #html = _nodes[i].outerHTML
      #if html and html.trim()
        #alert html
        #@items.push html
      #i++
    #return
  #created: ->
    #@items = []
    #return